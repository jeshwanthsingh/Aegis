# EXECUTION_LOG.md
## 2026-04-07 Initial /ai/ setup
- Created /ai/ workflow structure
- Completed: telemetry event bus, cgroup poller, SSE endpoint,
  containment receipt generation, end-to-end demo tests passing
- Pending: frontend UI, security audit

## 2026-04-08 Phase 1 - Schemas First
- Implemented: Phase 1 brief in `ai/CURRENT_PHASE.md`; added `schemas/intent-v1.json`, `schemas/event-v1.json`, `schemas/violation-v1.json`, `schemas/receipt-predicate-v1.json`; added `docs/v2/cedar-compilation-target.md` and `docs/v2/in-toto-mapping.md`
- Verified: JSON files parsed successfully with Python `json.tool`; schema surface checked against the locked v2 design and phase scope
- Skipped: runtime/backend changes, Cedar execution code, signing code, divergence logic, OTel export, attestation plumbing - deferred because Phase 1 is contract freeze only
- Assumptions: `gvisor` is a valid future-facing backend enum in v1 schemas; output artifacts remain canonically bound in in-toto `subject[]`; summary objects stay compact and do not attempt to encode raw event streams

## 2026-04-08 Phase 2 - Firecracker Sensor MVP
- Implemented: guest-side `/proc`-based runtime sensor scaffold in `guest-runner/runtime_sensor.go`; host-side RuntimeEvent normalization in `internal/executor/runtime_events.go`; vsock ingestion for batched runtime telemetry in `internal/executor/vsock.go`; internal RuntimeEvent model and telemetry kinds; integration demo script `tests/integration/runtime_events.sh`
- Verified: `/home/cellardoor/local/go/bin/go test ./...`; `cd guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `/home/cellardoor/local/go/bin/go build -buildvcs=false ./cmd/orchestrator`
- Skipped: live Firecracker execution verification on this machine - `scripts/preflight.sh` failed at `/dev/kvm` access because WSL requires interactive sudo for KVM access in the current environment
- Assumptions: Phase 2 uses the existing JSON chunk protocol with compact batched telemetry; host-side normalization owns `seq` and `dropped_since_last`; selected file/network visibility is sampled from guest `/proc` state rather than eBPF in this MVP


## 2026-04-08 Phase 2 - Firecracker validation debug pass
- Implemented: fixed `tests/integration/runtime_events.sh`; added `tests/integration/runtime_events_manual.sh`; added targeted runtime sensor status breadcrumbs in `guest-runner/runtime_sensor.go`, `internal/executor/runtime_events.go`, and `internal/telemetry/event.go`; refreshed the baked `guest-runner` inside `assets/alpine-base.ext4`; brought guest loopback up before user code in `guest-runner/main.go`
- Verified: `bash -n tests/integration/runtime_events.sh`; `bash -n tests/integration/runtime_events_manual.sh`; `/home/cellardoor/local/go/bin/go test ./internal/executor ./internal/telemetry ./internal/api ./internal/models`; `cd guest-runner && /home/cellardoor/local/go/bin/go test ./...`; real Firecracker runs emitted normalized `runtime.event.v1` with `process.exec`, `process.fork`, `process.exit`, and `file.open`
- Skipped: Phase 2 closure - `net.connect` is still not emitted reliably from the temporary `/proc` socket path, so the live validation is still provisional
- Assumptions: the original Phase 2 failure was partly caused by a stale `guest-runner` binary baked into the rootfs; loopback availability is part of the guest execution baseline; the current `/proc` socket visibility path is still scaffolding and not production-grade


## 2026-04-08 Phase 2 - ptrace guest sensor pass
- Implemented: added `guest-runner/runtime_trace.go` with a ptrace-based process/connect sensor for `process.exec`, `process.fork`, `process.exit`, and `net.connect`; updated `guest-runner/main.go` to launch the traced workload and wait on tracer-owned exit state; reduced `guest-runner/runtime_sensor.go` to `/proc`-based `file.open` fallback plus batching/drop accounting; updated `tests/integration/runtime_events.sh` and `tests/integration/runtime_events_manual.sh` to use a deterministic Python loopback connect probe; rebaked the new `guest-runner` into `assets/alpine-base.ext4`
- Verified: `bash -n tests/integration/runtime_events.sh`; `bash -n tests/integration/runtime_events_manual.sh`; `/home/cellardoor/local/go/bin/go test ./internal/executor ./internal/telemetry ./internal/api ./internal/models`; `cd guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `cd guest-runner && /home/cellardoor/local/go/bin/go build -buildvcs=false -a -o guest-runner .`
- Skipped: final real Firecracker proof on this machine - a fresh orchestrator instance now fails without privileged cgroup access (`open /sys/fs/cgroup/aegis/cgroup.subtree_control: permission denied`), and non-interactive sudo is unavailable here
- Assumptions: ptrace is the smallest production-minded direct sensor path that fits Phase 2 without introducing guest kernel tooling; `/proc` remains acceptable only as temporary `file.open` fallback

## 2026-04-08 Phase 2 - closure pass
- Implemented: added `scripts/rebake-guest-runner.sh` and baked `/etc/aegis-guest-runner.json` metadata into `assets/alpine-base.ext4`; made cgroup parent configurable in `internal/executor/lifecycle.go`; updated `cmd/orchestrator/main.go`, `scripts/preflight.sh`, `scripts/validate-phase2-runtime-events.sh`, `scripts/smoke-local.sh`, and `tests/integration/smoke.sh` to use the configurable parent; switched the canonical validation path to a delegated user scope so Firecracker keeps `/dev/kvm` access; tightened `guest-runner/main.go`, `guest-runner/runtime_trace.go`, and `tests/integration/runtime_events.sh` while debugging the real ptrace/connect path
- Verified: `./scripts/preflight.sh` passed; `./scripts/rebake-guest-runner.sh` rebaked the guest binary and emitted sha256/build-id metadata; `/home/cellardoor/local/go/bin/go test ./internal/executor ./internal/telemetry ./internal/api ./internal/models` passed after the cgroup-parent fix; `cd guest-runner && /home/cellardoor/local/go/bin/go test ./...` passed after the ptrace fixes; `bash -n scripts/validate-phase2-runtime-events.sh`; `bash -n scripts/smoke-local.sh`; `bash -n tests/integration/runtime_events.sh`; real Firecracker validation under `./scripts/validate-phase2-runtime-events.sh` proved `runtime.event.v1` for `process.exec`, `process.fork`, and `process.exit` using the freshly rebaked guest binary
- Skipped: Phase 2 closure; the real Firecracker proof still does not emit `net.connect`, so `tests/integration/runtime_events.sh` remains red by design and Phase 2 cannot honestly be marked complete
- Assumptions: `/proc` remains temporary `file.open` fallback only; delegated user scopes are the current local-dev path that preserves both cgroup delegation and `/dev/kvm` access; the remaining blocker is the AF_INET connect probe/sensor path rather than rootfs freshness or cgroup setup

## 2026-04-08 Phase 2 - AF_INET connect closure
- Implemented: tightened `guest-runner/runtime_trace.go` so child tracees treat plain `SIGTRAP`/`cause=0` stops as syscall stops; simplified `tests/integration/runtime_events.sh` to use a direct bash `/dev/tcp/127.0.0.1/17777` AF_INET connect attempt instead of the slower Python probe
- Verified: `cd guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `cd ~/aegis && ./scripts/validate-phase2-runtime-events.sh`; real Firecracker validation passed and proved `runtime.event.v1` for `process.exec`, `process.fork`, `process.exit`, and `net.connect`
- Skipped: all Phase 3 work and all non-Phase-2 cleanup
## 2026-04-08 Phase 3 - Intent point decisions
- Implemented: added `internal/policy/contract` for strict `IntentContract v1` loading/validation and internal model mapping; added `internal/policy/cedar` for internal principal/context compilation; added `internal/policy/evaluator` for point decisions on `process.exec`, `file.open`, and `net.connect`; added `internal/models/point_decision.go`; wired `policy.point.decision` emission into normalized runtime events in `internal/executor/runtime_events.go`; extended `internal/api/handler.go` to accept optional `intent` JSON and build a per-execution evaluator
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/policy/... ./internal/models`; `/home/cellardoor/local/go/bin/go test ./internal/executor -run "TestReadChunks"`; `/home/cellardoor/local/go/bin/go test ./internal/api`
- Skipped: sequence/divergence logic, kill logic, receipts, gVisor, attestation, OTel, and `file.open` source replacement - deferred because Phase 3 is point-decision groundwork only
- Notes: `file.open` is currently evaluated as read-only because RuntimeEvent v1 does not expose write intent cleanly; `net.connect` decisions are IP-based on the current Firecracker path unless a normalized event includes `domain`; a broader `go test ./internal/executor ./internal/api ./internal/models` run still hits an unrelated existing DNS telemetry test assumption outside this phase, so verification stayed targeted

## 2026-04-08 Phase 3 - validation closure
- Implemented: serialized package-level DNS allowlist hook overrides in `internal/executor/lifecycle_test.go` so the executor DNS tests no longer race each other under `t.Parallel()`
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/policy/... ./internal/models`; `/home/cellardoor/local/go/bin/go test ./internal/executor -run TestReadChunks`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`
- Skipped: all Phase 4 work, runtime behavior changes, and broader executor refactors because the remaining failure was test isolation only

## 2026-04-08 Phase 4 - Divergence Engine v0
- Implemented: added `internal/models/divergence.go`; added `internal/policy/divergence` with per-execution state, verdict escalation, and deterministic divergence rules for shell/package-install violations, process fan-out, read-oriented path scanning, repeated denied connects, and connect fan-out; integrated divergence evaluation into `internal/executor/runtime_events.go`, `internal/executor/vsock.go`, and `internal/api/handler.go`; added `policy.divergence.v1` telemetry kind and executor runtime-hook tests
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/policy/divergence ./internal/policy/... ./internal/models`; `/home/cellardoor/local/go/bin/go test ./internal/executor -run TestReadChunks`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`
- Skipped: enforcement/kill wiring, receipt changes, gVisor, attestation, OTel, ML, and any write-intent file semantics work because Phase 4 is execution-level judgment only
- Notes: No Phase 3.5 prerequisite was required; current `file.open` data is sufficient for read-oriented divergence rules but still not for write-specific claims

## 2026-04-08 Phase 4 - hardening pass
- Implemented: audited divergence thresholds and severity mapping in `internal/policy/divergence/evaluator.go`; tightened rule-hit messages to include concrete binaries, destinations, counts, and thresholds; added multi-event progression coverage in `internal/policy/divergence/evaluator_test.go` for `allow -> warn -> kill_candidate` and declared-target-touch suppression of workspace scan escalation
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/policy/divergence ./internal/policy/... ./internal/models`; `/home/cellardoor/local/go/bin/go test ./internal/executor -run TestReadChunks`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`
- Skipped: receipt work, enforcement, sensor changes, gVisor, and any write-intent file semantics work because this pass is Phase 4 hardening only

## 2026-04-08 Phase 5 - Signed execution receipts v1
- Implemented: added `internal/receipt` with receipt input types, Aegis execution predicate, in-toto Statement v1 generation, DSSE envelope generation, Ed25519 signing, verification, and artifact binding via `subject[]`; integrated signed receipt construction into `internal/api/handler.go` using the existing end-of-execution seam; kept legacy containment summaries for aggregate stats only
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/receipt`; `/home/cellardoor/local/go/bin/go test ./internal/policy/divergence ./internal/policy/... ./internal/models`; `/home/cellardoor/local/go/bin/go test ./internal/executor -run TestReadChunks`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`
- Skipped: host attestation, production key custody, gVisor, OTel, ML, and runtime artifact discovery wiring because Phase 5 is signed receipt groundwork only
- Notes: live execution receipts currently emit zero artifacts honestly unless artifacts are supplied explicitly to the receipt builder; signing defaults to a local/dev Ed25519 signer unless `AEGIS_RECEIPT_SIGNING_SEED_B64` is set


## 2026-04-08 Phase 6 - Proving Ground v2 / operator-facing proof flow
- Implemented: proof-bundle writing in `internal/receipt/bundle.go` and `internal/api/handler.go`; stdout/stderr artifact binding via `subject[]` for real executions; operator-facing `aegis receipt verify` flow in `cmd/aegis-cli/main.go`; proving scenarios in `scripts/run-proof-demo.sh`
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/receipt`; `/home/cellardoor/local/go/bin/go test ./cmd/aegis-cli`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`; `bash -n scripts/run-proof-demo.sh`
- Skipped: live proving-ground execution against a running orchestrator in this session; stream-response receipt surfacing; workspace file artifact discovery beyond captured stdio
- Notes: proof bundles now write `receipt.dsse.json`, `receipt.pub`, `receipt.summary.txt`, and honest stdout/stderr artifacts when present under `AEGIS_PROOF_ROOT` or `/tmp/aegis/proofs`; zero-artifact receipts remain valid when a run produces no stable captured output


## 2026-04-08 Phase 6 - operational closure
- Implemented: corrected proof-bundle surfacing in `internal/api/handler.go` so non-stream responses return the paths that are actually written; added `scripts/start-local-orchestrator.sh` using delegated `systemd-run --user --scope` startup; updated `scripts/run-proof-demo.sh` to fail fast with a clear orchestrator prerequisite
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/api ./cmd/aegis-cli ./internal/receipt`; `bash -n scripts/start-local-orchestrator.sh`; `bash -n scripts/run-proof-demo.sh`; `./scripts/start-local-orchestrator.sh`; real clean run via `curl -sS -X POST http://localhost:8080/v1/execute -H 'Content-Type: application/json' -d '{"lang":"bash","code":"echo phase6-basic","timeout_ms":10000}'`; real divergent run via bash intent that denies spawned `/bin/sh`; both receipts verified with `go run ./cmd/aegis-cli receipt verify ...`
- Skipped: broader artifact collection beyond stdout/stderr; stream-response proof surfacing; host attestation and key-custody work
- Notes: the original blocker was a combination of interactive-sudo assumptions and a false proof-path surfacing bug; the working local operator path is now the delegated user-scope startup helper plus non-stream execute responses that surface actual bundle files

## 2026-04-08 Phase 7 - Operational Proof Hardening v1
- Implemented: broadened proof-bundle artifacts with deterministic `output-manifest.json`; added `ArtifactsFromBundleOutputs` and proof-bundle path resolution in `internal/receipt`; added stream proof metadata chunks and CLI proof-path printing; extended `aegis receipt verify` with `--execution-id` / `--proof-dir`; added `aegis receipt show`; added explicit runtime-baseline file-read handling for launcher/runtime noise reduction
- Verified: `/home/cellardoor/local/go/bin/gofmt -w internal/receipt/bundle.go internal/receipt/builder_test.go internal/models/types.go internal/policy/evaluator/evaluator.go internal/policy/evaluator/evaluator_test.go internal/api/handler.go cmd/aegis-cli/main.go cmd/aegis-cli/main_test.go`; `/home/cellardoor/local/go/bin/go test ./internal/receipt`; `/home/cellardoor/local/go/bin/go test ./cmd/aegis-cli`; `/home/cellardoor/local/go/bin/go test ./internal/policy/evaluator`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`; `bash -n scripts/run-proof-demo.sh`; live non-stream `go run ./cmd/aegis-cli run --lang bash --code 'echo phase7-nonstream'`; live stream `go run ./cmd/aegis-cli run --lang bash --code 'echo phase7-stream' --stream`; live `go run ./cmd/aegis-cli receipt verify --file ...`; live `go run ./cmd/aegis-cli receipt show --execution-id ...`; live `go run ./cmd/aegis-cli receipt verify --execution-id ...`
- Skipped: workspace-file extraction from the guest, host attestation, production key custody, gVisor, OTel/export, and any receipt-format changes beyond the new surfaced manifest artifact because Phase 7 is proof hardening only

## 2026-04-08 Phase 8 - Production Trust Hardening v1
- Implemented: explicit receipt signing modes in `internal/receipt/signer.go`; strict key loading/validation and fail-closed behavior; receipt trust posture fields in the Aegis predicate; trust-aware verification summary lines in `internal/receipt/bundle.go`; API receipt path updated to use explicit signer loading and to return clear receipt-signing errors instead of silent fallback behavior
- Verified: `/home/cellardoor/local/go/bin/gofmt -w internal/receipt/signer.go internal/receipt/signer_test.go internal/receipt/types.go internal/receipt/builder.go internal/receipt/verify.go internal/receipt/bundle.go internal/receipt/builder_test.go internal/api/handler.go cmd/aegis-cli/main_test.go`; `/home/cellardoor/local/go/bin/go test ./internal/receipt`; `/home/cellardoor/local/go/bin/go test ./cmd/aegis-cli`; `/home/cellardoor/local/go/bin/go test ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`
- Skipped: host attestation, production key custody, HSM/KMS integration, gVisor, OTel/export, and any receipt-format redesign beyond explicit trust posture fields because Phase 8 is trust hardening only


## 2026-04-08 Phase 9 - Kinetic Enforcement + Write-Truth v1
- Implemented: added file write-intent semantics in `internal/models/runtime_event.go`, `guest-runner/runtime_sensor.go`, and `internal/policy/evaluator/evaluator.go`; added write-aware divergence state and rules in `internal/policy/divergence/state.go` and `internal/policy/divergence/evaluator.go`; wired real containment from divergence `kill_candidate` through `internal/executor/runtime_events.go`, `internal/executor/vsock.go`, `internal/executor/firecracker.go`, and `internal/api/handler.go`; added focused tests in `guest-runner/runtime_sensor_test.go`, `internal/policy/evaluator/evaluator_test.go`, `internal/policy/divergence/evaluator_test.go`, and `internal/executor/vsock_test.go`
- Verified: `/home/cellardoor/local/go/bin/gofmt -w internal/models/runtime_event.go internal/models/divergence.go internal/policy/evaluator/evaluator.go internal/policy/evaluator/evaluator_test.go internal/policy/divergence/state.go internal/policy/divergence/evaluator.go internal/policy/divergence/evaluator_test.go guest-runner/runtime_sensor.go guest-runner/runtime_sensor_test.go internal/telemetry/event.go internal/executor/runtime_events.go internal/executor/vsock.go internal/executor/vsock_test.go internal/executor/firecracker.go internal/api/handler.go`; `/home/cellardoor/local/go/bin/go test ./internal/policy/evaluator ./internal/policy/divergence ./internal/models ./internal/receipt`; `cd guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `/home/cellardoor/local/go/bin/go test ./internal/executor ./internal/api`; `/home/cellardoor/local/go/bin/go test ./internal/...`; `./scripts/rebake-guest-runner.sh`; real clean Firecracker run with `lang=bash` completed normally; real divergent Firecracker run reached actual containment with `exit_reason=divergence_terminated`; both live receipts verified with `go run ./cmd/aegis-cli receipt verify ...`
- Skipped: no secret broker, no host attestation, no gVisor, no receipt-format redesign, and no replacement of the temporary `/proc` file-open source beyond adding real open-flag parsing because this phase is write-truth plus kinetic containment only

## 2026-04-08 Phase 10 - Stronger File Sensor / Better Write Truth v1
- Implemented: replaced the live `/proc`-based `file.open` source with direct ptrace syscall capture for `open`, `openat`, and `openat2` in `guest-runner/runtime_trace.go`; resolved relative file paths against tracee cwd/dirfd; removed the `/proc` poller from the live guest sensor startup path in `guest-runner/runtime_sensor.go`; added focused tracer tests in `guest-runner/runtime_trace_test.go`; tightened executor normalization coverage for ptrace-sourced file-open flags in `internal/executor/vsock_test.go`
- Verified: `/home/cellardoor/local/go/bin/gofmt -w guest-runner/runtime_trace.go guest-runner/runtime_trace_test.go guest-runner/runtime_sensor.go internal/executor/vsock_test.go`; `cd ~/aegis/guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/policy/evaluator ./internal/policy/divergence ./internal/models ./internal/receipt`; `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/executor ./internal/api`; `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/...`; `cd ~/aegis && ./scripts/rebake-guest-runner.sh`; real clean Firecracker run `55555555-5555-4555-8555-555555555555` completed with `divergence_verdict=allow` and verified receipt; real denied-write Firecracker run `44444444-4444-4444-8444-444444444444` triggered `file.write_outside_scope` / `file.destructive_open_flags`, enforced VM kill, and verified receipt
- Skipped: completed-write proof, mmap/write-after-open tracking, secret broker work, host attestation, and any receipt-format or backend work because this phase is file sensor truth only

## 2026-04-08 Phase 11 - Credential Broker over vsock v1 live validation debug
- Implemented: resumed from the live validation blocker only; inspected broker implementation, live proof bundles, orchestrator logs, and guest/runtime behavior; added targeted broker debug breadcrumbs in `guest-runner/broker_proxy.go` and `internal/executor/proxy.go`; rebaked `guest-runner`; restarted the orchestrator with `AEGIS_CRED_GITHUB_TOKEN`; stood up a local upstream validation service that logs host-side `Authorization` usage without echoing the token back in the response body
- Verified: `/home/cellardoor/local/go/bin/go test ./guest-runner/...`; `/home/cellardoor/local/go/bin/go test ./internal/executor ./internal/broker`; `./scripts/rebake-guest-runner.sh`; real loopback probe run `429f333c-1f7b-443b-b2ef-21d9431e29c5` completed with `net.connect` to `127.0.0.1:8888` and verified receipt; real brokered HTTP run `0d617ff5-35ba-4a60-ad88-87f8cd996781` returned explicit `502 broker unavailable: dial vsock hypervisor(0):1025: connect: connection timed out`
- Skipped: final allowed brokered request success, denied brokered request success, and final broker completion claim because the guest-side broker vsock dial target is still wrong on the live path
- Notes: the original allowed-flow kill was caused by real policy noise (`process.shell_disallowed` from launcher bash and `file.write_outside_scope` on `/dev/tty`), but after widening the intent honestly the remaining blocker is not file policy; it is the guest proxy dialing the wrong vsock CID for brokered HTTP

## 2026-04-09 Phase 11 - Credential Broker over vsock v1 live validation (session 2)
- Implemented: identified and fixed guest-side vsock CID bug (vsock.Hypervisor CID=0 to vsock.Host CID=2) in guest-runner/broker_proxy.go; added PYTHONHASHSEED=0 to subprocess env in guest-runner/main.go; rebaked guest-runner (sha256: 8ecbe7585a6053404788c0944e19ffa319d0ed7c955567d5c04bc2e066fb1c43); diagnosed entropy starvation (getrandom blocking on kernel 4.14.174, no virtio_rng) as root cause of both Python and curl hang in the guest; switched validation harness to bash + /dev/tcp to bypass the entropy issue entirely
- Verified: guest-runner sha256 confirmed in live runtime_sensor_status log; bash + /dev/tcp allowed probe execution a1b2c3d4-e5f6-4789-8abc-def012345602 completed with exit_reason=completed, stdout PASS_auth_present_no_raw_token, duration 3277ms; orchestrator logs confirm vsock_connected, broker_listener_started, correct process ancestry
- Skipped: orchestrator telemetry grep for credential.request / credential.allowed events (in progress when session stopped); upstream log confirmation that Authorization header was injected; receipt verification for the successful allowed run; denied-path live validation; final phase closure
- Notes: broker architecture is not broken; the brokered HTTP path works end to end with the bash /dev/tcp harness; remaining work is evidence confirmation and denied-path closure only; do not use Python or curl as test client on this guest kernel -- both hang due to entropy starvation in libssl/libgnutls on kernel 4.14.174; policy noise sources are fully mapped in CURRENT_PHASE.md

## 2026-04-09 Phase 11 - Credential Broker over vsock v1 operational closure
- Implemented: confirmed prior documented allowed run was a false positive caused by a wrong delegation name (GITHUB_TOKEN vs loaded binding github) plus a loose guest response matcher; added operator-visible broker event logging in internal/broker/broker.go; fixed guest broker proxy startup race in guest-runner/broker_proxy.go and guest-runner/main.go by waiting for the loopback listener before launching user code; rebaked guest-runner; completed fresh allowed and denied real Firecracker broker validations
- Verified: /home/cellardoor/local/go/bin/go test ./internal/broker; cd ~/aegis/guest-runner && /home/cellardoor/local/go/bin/go test ./...; ./scripts/rebake-guest-runner.sh; real allowed run b12cd34-ef56-4789-8abc-def012345699 completed with PASS_auth_present_true_no_raw_token; orchestrator log showed credential.request + credential.allowed; upstream log showed Authorization=Bearer phase11-test-token; go run ./cmd/aegis-cli receipt verify --proof-dir /tmp/aegis/proofs/ab12cd34-ef56-4789-8abc-def012345699 returned verified; real denied run c23de45-fa67-4789-8abc-def012345700 completed with PASS_denied_no_raw_token; orchestrator log showed credential.request + credential.denied; upstream log remained empty; go run ./cmd/aegis-cli receipt verify --proof-dir /tmp/aegis/proofs/bc23de45-fa67-4789-8abc-def012345700 returned verified
- Skipped: broker architecture redesign, broader secret mediation, receipt format changes, trust-hardening beyond existing modes, and any policy weakening outside the exact demo intent because this session was operational closure only

## 2026-04-09 Phase 12 - Setup Simplification
- Implemented: added `internal/config` for repo-local config generation/loading; added `internal/setup` for idempotent bootstrap and structured readiness checks; added `internal/serve` for startup planning; extended `cmd/aegis-cli/main.go` with `setup` and `serve`; updated `internal/executor/firecracker.go` to honor `AEGIS_FIRECRACKER_BIN`; updated `README.md` quickstart; ignored generated `.aegis/` local state in `.gitignore`
- Verified: `/home/cellardoor/local/go/bin/go test ./cmd/aegis-cli ./internal/config ./internal/setup ./internal/serve`; `/home/cellardoor/local/go/bin/go test ./cmd/aegis-cli ./internal/serve ./internal/executor ./cmd/orchestrator`; live `go run ./cmd/aegis-cli setup`; live `./.aegis/bin/aegis serve` + `./.aegis/bin/aegis health`; live `./.aegis/bin/aegis run --lang bash --code 'echo setup-phase-demo'` with proof bundle output
- Skipped: broker-capable live demo validation in this phase because `AEGIS_CRED_GITHUB_TOKEN` was intentionally unset on the validation host; network privilege automation remains explicit rather than hidden

## 2026-04-09 Phase 13 - CLI / Runtime Stabilization Review
- Implemented: reviewed the setup/serve/config/runtime boundary; added stale repo-local binary freshness checks and stale-binary operator messaging in `internal/setup/setup.go` and `cmd/aegis-cli/main.go`; added explicit API-auth and startup-mode readiness reporting; made serve output distinguish broker-demo readiness, API auth posture, and receipt-signing posture; hardened freshness scanning to ignore missing optional source roots cleanly; extended setup tests for auth and stale-binary review cases
- Verified: `/home/cellardoor/local/go/bin/go test ./cmd/aegis-cli ./internal/setup ./internal/serve ./internal/config ./internal/executor ./cmd/orchestrator`; live `go run ./cmd/aegis-cli setup`; live `./.aegis/bin/aegis serve`; live `./.aegis/bin/aegis health`; live `./.aegis/bin/aegis run --lang bash --code 'echo sdk-review-demo-2'` with proof bundle output
- Skipped: broker-capable live smoke validation because `AEGIS_CRED_GITHUB_TOKEN` was intentionally unset on the review host; no broad refactor or architecture changes by design

## 2026-04-09 Phase 14 - Go API Seam Freeze
- Implemented: added versioned `GET /v1/health`; added shared JSON error-envelope helpers in `internal/api/errors.go`; normalized non-2xx SDK-facing failures in `internal/api/handler.go`; tightened execute request decoding to reject unknown fields; updated `cmd/aegis-cli/main.go` to use `/v1/health` and parse the new error envelope; added `docs/api_contract.md`; added focused handler and CLI tests for auth/error and versioned-health behavior
- Verified: `/home/cellardoor/local/go/bin/go test ./internal/api ./cmd/aegis-cli ./cmd/orchestrator`; live `go run ./cmd/aegis-cli setup`; live `./scripts/start-local-orchestrator.sh`; live `curl -sS http://localhost:8080/v1/health`; live sync execute against `POST /v1/execute`; live invalid-request execute returning `400` JSON error envelope; live stream execute against `POST /v1/execute/stream`; live authenticated-mode check with temporary `AEGIS_API_KEY=phase14-token` scope showing `401 auth_required` without bearer token and successful bearer-auth execute; live `./.aegis/bin/aegis health` and `./.aegis/bin/aegis run --lang bash --code 'echo cli-after-freeze'` against the authenticated server
- Skipped: async polling/status endpoints, receipt-download endpoints, telemetry/workspace/stats API freezing, broker-specific validation, and any runtime architecture redesign because this phase was limited to the SDK-facing HTTP seam

## 2026-04-09 Phase 15 - Python SDK v1
- Implemented: added `sdk/python` package with `AegisClient`, standard-library HTTP transport, typed result and health models, typed intent dataclasses, SSE stream event parsing, receipt wrapper and CLI-backed verifier, package README, runnable examples, and focused unit tests; added root README pointer to the SDK docs
- Verified: `python3 -m py_compile sdk/python/aegis/*.py sdk/python/examples/*.py sdk/python/tests/test_sdk.py`; `cd ~/aegis && python3 -m venv sdk/python/.venv`; `cd ~/aegis && sdk/python/.venv/bin/python -m unittest discover -s sdk/python/tests -t sdk/python -v`; `cd ~/aegis && sdk/python/.venv/bin/pip install -e sdk/python`; live import via `sdk/python/.venv/bin/python -c 'import aegis; print(aegis.AegisClient().__class__.__name__)'`; live `go run ./cmd/aegis-cli setup`; live local orchestrator via `./scripts/start-local-orchestrator.sh`; live SDK health/sync execute/execution-failure/receipt-verify/stream run; live authenticated SDK check against temporary `AEGIS_API_KEY=phase14-token` scope showing `AegisAuthError` on missing bearer and successful bearer-auth execute
- Skipped: no server API redesign, no async/status polling surface, no MCP wrapper, no broker-specific SDK additions, and no Python-native receipt crypto because this phase stayed scoped to an HTTP client SDK against the frozen server contract


## 2026-04-09 Phase 16 - Python SDK broker smoke
- Implemented: added `sdk/python/examples/_broker_smoke_common.py`, `sdk/python/examples/broker_allowed.py`, and `sdk/python/examples/broker_denied.py` to drive the canonical broker allowed/denied PASS markers through the Python SDK using the existing frozen execute API; added gated live integration coverage in `sdk/python/tests/test_broker_smoke.py`; documented broker smoke prerequisites and rerun steps in `sdk/python/README.md`
- Verified: `python3 -m py_compile sdk/python/aegis/*.py sdk/python/examples/*.py sdk/python/tests/test_sdk.py sdk/python/tests/test_broker_smoke.py`; `python3 -m venv sdk/python/.venv`; `sdk/python/.venv/bin/pip install -e sdk/python`; `sdk/python/.venv/bin/python -m unittest discover -s sdk/python/tests -t sdk/python -v` passed with the broker smoke test correctly skipped unless explicitly enabled; `/home/cellardoor/local/go/bin/go run ./cmd/aegis-cli setup`; restarted local orchestrator via `./scripts/start-local-orchestrator.sh`; attempted live `sdk/python/.venv/bin/python sdk/python/examples/broker_allowed.py` and `sdk/python/.venv/bin/python sdk/python/examples/broker_denied.py`, both failing fast with the explicit missing-exported-credential error
- Skipped: real allowed/denied broker proof because `AEGIS_CRED_GITHUB_TOKEN` was not available as an exported WSL environment variable for the local orchestrator/examples; no broker/server redesign because this phase stays on smoke validation only


## 2026-04-09 Phase 16 - Python SDK broker smoke follow-up
- Implemented: patched `sdk/python/examples/_broker_smoke_common.py` so the broker smoke intent now allows the minimal live bash startup behavior seen in the orchestrator log (`read_paths` widened to include `/etc`, `/usr/share/locale`, and `/dev`; `write_paths` widened to include `/dev/tty`; `max_distinct_files` raised from 4 to 64); added a helper fallback that can read `AEGIS_CRED_GITHUB_TOKEN` from the running local orchestrator process for secret-leak checks when the caller shell is not exported
- Verified: direct inspection of `/tmp/aegis-local-orchestrator.log` confirmed the previous broker smoke failures were due to `file.write_outside_scope` on `/dev/tty`, denied locale/passwd reads, and `file.distinct_limit_exceeded`; `python3 -m py_compile sdk/python/examples/_broker_smoke_common.py`; direct helper check proved `_secret_from_orchestrator_env()` could recover the token from the running orchestrator while it was alive; reran `sdk/python/.venv/bin/python sdk/python/examples/broker_allowed.py` and `sdk/python/.venv/bin/python sdk/python/examples/broker_denied.py`, which advanced past the original missing-env blocker and then stalled on live rerun because the local server/orchestrator died before current-post-patch validation could complete
- Skipped: final allowed/denied broker pass claim, because once the credentialed orchestrator died the current WSL execution environment could not honestly restart it with a real exported `AEGIS_CRED_GITHUB_TOKEN`


## 2026-04-09 Phase 16 - Python SDK broker smoke follow-up 2
- Implemented: patched `sdk/python/examples/_broker_smoke_common.py` again to stop overconstraining the broker smoke on network/process behavior; `network_scope` now allows only the specific localhost path needed for the guest-to-proxy connection (`allow_network=true`, `allowed_ips=['127.0.0.1']`, `max_outbound_conns=1`, `max_dns_queries=0`); `process_scope.max_child_processes` raised to 6; guest allowed/denied scripts now use bash builtins instead of spawning `cat` and `grep`
- Verified: `python3 -m py_compile sdk/python/examples/_broker_smoke_common.py`; direct source inspection confirmed the patched localhost-only network scope, wider child budget, and builtin-only guest scripts; `curl -fsS http://localhost:8080/v1/health` now fails because the local server is down, so no honest post-patch live broker rerun could complete in this session
- Skipped: final allowed/denied broker pass claim, because the credentialed local orchestrator is no longer running and the current WSL child-process environment still does not expose a real exported `AEGIS_CRED_GITHUB_TOKEN` for a truthful restart


## 2026-04-09 Phase 16 - Python SDK broker smoke closure
- Implemented: added SDK broker smoke examples and gated integration coverage; fixed `_broker_smoke_common.py` so the smoke intent no longer overconstrains bash startup file behavior, the localhost broker-proxy path, or child-process budget; removed external `cat` / `grep` usage from the guest scripts; replaced the broken host-log telemetry assertion with broker evidence that actually exists for this runtime (`upstream_probe_auth_present` on allowed runs and `receipt:broker.request_denied` on denied runs), while still using host telemetry when an operator log sink is configured
- Verified: `python3 -m py_compile sdk/python/aegis/*.py sdk/python/examples/*.py sdk/python/tests/test_sdk.py sdk/python/tests/test_broker_smoke.py`; `sdk/python/.venv/bin/python -m unittest discover -s sdk/python/tests -t sdk/python -v`; live `sdk/python/.venv/bin/python sdk/python/examples/broker_allowed.py` passed with execution_id `60c79e8d-96f8-4471-80ba-27994305e976`, `result_ok=true`, `upstream_requests=1`, `receipt_verified=true`, and `telemetry=upstream_probe_auth_present`; live `sdk/python/.venv/bin/python sdk/python/examples/broker_denied.py` passed with execution_id `361464e9-a688-4063-82f2-74606a10472f`, `result_ok=true`, `upstream_requests=0`, `receipt_verified=true`, and `telemetry=receipt:broker.request_denied`; matching proof bundles under `/tmp/aegis/proofs/60c79e8d-96f8-4471-80ba-27994305e976` and `/tmp/aegis/proofs/361464e9-a688-4063-82f2-74606a10472f` contain the canonical PASS markers and verified receipts
- Skipped: no broker/server redesign, no new public API surface, and no attempt to force a synthetic host telemetry sink into the local runtime just to satisfy the smoke harness


## 2026-04-09 Phase 17 - Python SDK v1 polish
- Implemented: added a cleaner public SDK surface around `AegisClient`, `ExecutionRequest`, `ExecutionResult`, `Receipt`, and a small error hierarchy; added primary `client.run(...)` / `client.stream(...)` methods while keeping compatibility aliases; added result/client receipt-verification helpers and explicit `AegisConfigurationError`; updated SDK docs/examples to reflect the real install story, naming, local-dev posture, and broker smoke flow; kept broker examples aligned to the finalized public `run(...)` API
- Verified: `cd ~/aegis && python3 -m py_compile sdk/python/aegis/*.py sdk/python/examples/*.py sdk/python/tests/test_sdk.py sdk/python/tests/test_broker_smoke.py`; `cd ~/aegis && sdk/python/.venv/bin/python -m unittest discover -s sdk/python/tests -t sdk/python -v`; `cd ~/aegis && sdk/python/.venv/bin/pip install -e sdk/python`; live SDK script proving import, public request object, `health()`, `run()`, and `verify_receipt()` against the local server; live `sdk/python/.venv/bin/python sdk/python/examples/broker_allowed.py`; live `sdk/python/.venv/bin/python sdk/python/examples/broker_denied.py`
- Skipped: no server API redesign, no TypeScript SDK work, no MCP layer, and no Python-native receipt crypto implementation because this phase stayed on Python SDK v1 usability only

## 2026-04-09 Phase 18 - TypeScript SDK v1
- Implemented: created `sdk/typescript` with `package.json`, `tsconfig.json`, `README.md`, source modules for client/request/result/receipt/errors/verifier/stream handling, example files for health/run/stream/verify/authenticated/broker allowed/broker denied, and unit-test files for core SDK behavior plus gated broker smoke coverage; mirrored the Python SDK v1 public surface and kept receipt verification CLI-backed for parity
- Verified: inspected the in-repo package file set under `sdk/typescript`; confirmed `npm install` completed successfully in the package; confirmed a local orchestrator restart with broker credential env for later live validation; confirmed the phase is currently blocked on TypeScript build invocation rather than package scaffolding
- Skipped: honest TypeScript SDK closure because `dist/` has not been emitted yet, built tests have not run yet, and no built live examples or broker smoke runs have completed; environment-specific WSL + Windows Node build invocation remains unresolved

## 2026-04-09 Phase 18 - TypeScript SDK v1 closure
- Implemented: completed the Node-first TypeScript SDK under `sdk/typescript` with aligned client/request/result/receipt/error concepts, examples, tests, WSL-aware CLI receipt verification from Windows Node, and WSL-hosted broker probe handling for the public broker smoke examples
- Verified: `cmd /c "pushd \\wsl.localhost\Ubuntu\home\cellardoor\aegis\sdk\typescript && npm run build && popd"`; `cmd /c "pushd \\wsl.localhost\Ubuntu\home\cellardoor\aegis\sdk\typescript && npm test && popd"`; `wsl bash -lc "cd ~/aegis && export AEGIS_CRED_GITHUB_TOKEN=phase11-test-token && ./scripts/start-local-orchestrator.sh"`; `cmd /c "set AEGIS_BASE_URL=http://172.30.137.226:8080&& pushd \\wsl.localhost\Ubuntu\home\cellardoor\aegis\sdk\typescript && node dist/examples/health.js && node dist/examples/run_code.js && node dist/examples/verify_receipt.js && node dist/examples/run_stream.js && popd"`; `cmd /c "set AEGIS_BASE_URL=http://172.30.137.226:8080&& pushd \\wsl.localhost\Ubuntu\home\cellardoor\aegis\sdk\typescript && node dist/examples/broker_allowed.js && node dist/examples/broker_denied.js && popd"` with passing allowed/denied broker results and verified receipts
- Skipped: npm publish flow, browser support, TypeScript-native receipt crypto, MCP work, and any server/runtime redesign because this phase stayed on TypeScript SDK v1 only

## 2026-04-09 Phase 19 - MCP Server Wrapper v1
- Implemented: added cmd/aegis-mcp and internal/mcp as a real stdio MCP server over the existing Aegis HTTP seam; implemented initialize, initialized-notification handling, 	ools/list, 	ools/call, egis_execute, and egis_verify; compiled a narrow v1 intent from simplified MCP inputs with network disabled by default, no broker delegation by default, and only the minimal validated bash runtime allowances; verified receipts with the native Go receipt verifier; added focused protocol/tool tests; added docs/mcp_server.md plus README pointer
- Verified: /home/cellardoor/local/go/bin/go test ./internal/mcp ./cmd/aegis-mcp; /home/cellardoor/local/go/bin/go build -buildvcs=false -o ./.aegis/bin/aegis-mcp ./cmd/aegis-mcp; live stdio initialize + 	ools/list against ./.aegis/bin/aegis-mcp; live stdio 	ools/call for egis_execute with bare ash code echo mcp-smoke; live stdio 	ools/call for egis_verify against the emitted proof bundle; live stdio safe-default denial using bare egis_execute + /dev/tcp/127.0.0.1/9 proving 
etwork.connect_disabled; real Claude Code validation after registering the server via a Windows launcher: Claude successfully called egis_execute and returned execution_id e44e029f-19fc-48f3-b0a5-6135946b5186, then successfully called egis_verify for that execution and returned erified=true
- Skipped: prompts/resources support, pagination/change-notification work, Windows-native proof-path translation, and any server/runtime redesign because this phase stayed on the MCP wrapper only

## 2026-04-09 Phase 20 - Warm Pool v1
- Implemented: added `internal/pool` as the warm-pool lifecycle manager with background preboot, pause, claim, replenish, status, and max-age recycle logic; added a dedicated guest readiness vsock listener so warm VMs can be proven ready before pause; added executor pause/resume helpers plus separate cgroup naming so pooled VMs do not need to be born with the future execution ID; integrated warm-claim-or-cold-fallback into both `/v1/execute` and `/v1/execute/stream`; surfaced warm-pool config in `config`, `serve`, `/health`, `/ready`, `/metrics`, and README/docs; kept v1 honest by only serving default-profile scratch executions warm while workspace-backed or non-default-profile requests cold-fallback
- Verified: `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/pool ./internal/api ./internal/executor ./cmd/orchestrator ./cmd/aegis-cli`; `cd ~/aegis/guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `cd ~/aegis && /home/cellardoor/local/go/bin/go build -buildvcs=false ./...`; `cd ~/aegis && /home/cellardoor/local/go/bin/go run ./cmd/aegis-cli setup`; live scripted validation with cold server (`warm_pool_size=0`) and warm server (`warm_pool_size=1`) showed `payload_dispatch_start` cold baseline `2430ms` vs warm `15ms`, warm `/health` moving from `initializing=1` to `available=1`, proof bundles still emitted on warm runs, pool-empty cold fallback under load (`fallback_path=cold` while warm pool remained enabled), 10 rapid sequential executions all succeeding, Python SDK broker allowed example passing against the warm-pool server with `receipt_verified=true`, and an idle `warm_pool_max_age=2` server incrementing `recycled_expired` from `0` to `1`
- Skipped: workspace-backed and non-default-profile warm claims, because the current VM creation seam binds scratch/workspace disks and compute profile at boot time and this phase stayed on the small pause/resume pool rather than redesigning that runtime contract

## 2026-04-10 Phase 21 - Security Hardening & Adversarial Testing
- Implemented: added bounded live adversarial coverage in `scripts/run_adversarial_hardening.py` for TOCTOU symlink races, telemetry flooding, CPU and memory exhaustion, receipt and manifest tampering, ghost artifact injection, config mount protection, stream/non-stream parity, and broker denied-path regression; hardened guest-side syscall tracing in `guest-runner/runtime_trace.go` so symlink opens on writable mounts are detected and killed before the open resumes; hardened proof verification in `internal/receipt/bundle.go`, `cmd/aegis-cli/main.go`, and `internal/mcp/tools.go` so proof-dir verification recomputes artifact digests, rejects unexpected files, and validates the output manifest against the signed subject set; added focused tests for the new receipt and tracer behavior plus a write-intent flag matrix in `internal/policy/evaluator/evaluator_test.go`
- Verified: `cd ~/aegis/guest-runner && /home/cellardoor/local/go/bin/go test ./...`; `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/receipt ./internal/policy/evaluator ./cmd/aegis-cli ./internal/mcp`; `cd ~/aegis && /home/cellardoor/local/go/bin/go run ./cmd/aegis-cli setup`; live pre-fix symlink race reproduced `/etc/passwd` leakage via `SYMLINK_BYPASS:root:x:0:0:root:/root:/bin/bash`; live post-fix symlink rerun blocked the read and logged `blocked-symlink-open`; live pre-fix tampered `output-manifest.json` still verified via `aegis receipt verify --proof-dir`; live post-fix tampered receipt, tampered manifest, and ghost artifact copies all failed verification; final live `python3 scripts/run_adversarial_hardening.py` returned all-pass results for Tier 1 plus Tier 2/Tier 3 coverage, including healthy host status after telemetry flood and time-bounded CPU/memory abuse, stream/non-stream proof parity, read-only config mount denial, and a broker denied-path run with `PASS_denied_no_raw_token`
- Skipped: real broker allowed-path regression with a live upstream secret, because `AEGIS_CRED_GITHUB_TOKEN` was not present in the current host/orchestrator environment; graphify rebuild still failed with `maximum recursion depth exceeded`

## 2026-04-10 Phase 21 - Security hardening follow-up
- Implemented: promoted tracer-triggered symlink denials into explicit enforced execution outcomes in `internal/executor/runtime_events.go`, emitting `security_denied_symlink_open` plus a policy-enforcement event instead of leaving the run looking generically `completed`; updated `internal/api/handler.go` classification so security-denied runs persist as contained `security_denied`; added focused coverage in `internal/executor/vsock_test.go` and `internal/api/handler_test.go`
- Verified: `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/executor ./internal/api`; rebuilt local binaries with `cd ~/aegis && /home/cellardoor/local/go/bin/go run ./cmd/aegis-cli setup`; restarted the local orchestrator and reran the live symlink race, which returned `exit_reason=security_denied_symlink_open` for execution `ae630361-97c8-46ce-ab51-2438bd064c4b`, and its signed proof summary now shows `outcome=security_denied_symlink_open exit_code=137`; restarted the orchestrator with scoped env `AEGIS_CRED_GITHUB_TOKEN=phase21-test-token`, verified the serving process held that env, and ran `PYTHONPATH=./sdk/python AEGIS_BASE_URL=http://127.0.0.1:8080 python3 sdk/python/examples/broker_allowed.py`, which passed with execution `61abb459-93cf-47ee-9bb8-c4c3ecd11b25`, `receipt_verified=true`, `upstream_requests=1`, and `telemetry=credential.request,credential.allowed`
- Skipped: graphify rebuild still fails independently with `maximum recursion depth exceeded`

## 2026-04-10 Code Quality & Review Hardening
- Implemented: added a strict `.golangci.yml`; fixed real static-analysis findings in `internal/executor/lifecycle.go`, `internal/executor/vsock.go`, `internal/executor/proxy.go`, `internal/mcp/server.go`, `internal/policy/divergence/evaluator.go`, `internal/setup/setup.go`, `internal/api/handler.go`, and `cmd/aegis-mcp/main.go`; tightened Python and TypeScript SDK lint config and fixed the surfaced Ruff/mypy/ESLint/type issues; added focused Go tests for receipt verification, broker request handling, divergence rules, policy evaluator branches, and API helper/error/proof paths; extracted strict broker and receipt JSON decoders and wired them into production paths; added and ran fuzz targets for intent contracts, broker requests, and receipt DSSE envelopes; audited panic/ignored-error/sensitive-data surfaces and fixed the new executor `fmt.Errorf(chunk.Error)` bug plus non-silent JSON encoding in `internal/api/errors.go`
- Verified: Go static gate clean via `golangci-lint run ./...`, `go vet ./...`, and `staticcheck ./...`; Python SDK clean via `ruff check .` and `mypy --strict aegis`; TypeScript SDK clean via `npm run lint` and `npx tsc -p tsconfig.json --noEmit`; dependency checks run via `go mod tidy`, `go mod verify`, `govulncheck ./...`, `sdk/python/.venv/bin/pip-audit`, and `npm audit --audit-level=low`; targeted Go tests passed for `./internal/receipt ./internal/broker ./internal/policy/divergence ./internal/policy/evaluator ./internal/api ./internal/executor ./internal/policy/contract`; fuzz runs passed for `FuzzLoadIntentContractJSON`, `FuzzDecodeBrokerRequestJSON`, and `FuzzParseSignedReceiptJSON` at `-fuzztime=30s` each; Python SDK coverage measured at `85%` total via `coverage run -m unittest discover -s tests` and `coverage report -m aegis/*.py`; TypeScript SDK coverage measured at `78.79%` statements via `npx c8 --reporter=text node --test dist/tests/*.test.js`; critical Go function coverage improved to `VerifySignedReceipt=100%`, `broker.Handle=100%`, `ObserveBrokerDenial=100%`, `EvaluateFileAccess=100%`, `evaluateExec=95.5%`, `evaluateConnect=94.4%`, and API helper/proof functions mostly `>90%`
- Skipped: no broad subsystem refactors, no feature work, and no attempt to hide remaining quality gaps with blanket suppressions; overall launch-quality threshold was not claimed because API execute/stream handler coverage remains far below the requested bar, Python/TypeScript public-SDK coverage remains below `90%`, and the production tree still contains many ignored-error sites outside the small security-relevant slice fixed here

## 2026-04-10 Code Quality & Review Hardening closeout
- Implemented: raised the real remaining gate blockers with focused tests and localized runtime fixes; hardened `internal/api/handler.go` with explicit JSON/SSE/audit-write error handling plus narrow test seams; covered `NewHandler` and `NewStreamHandler` happy/error/auth/conflict/request-size/sandbox-failure paths; fixed silent JSON/SSE writes in `internal/api/observability.go`, `internal/api/stats.go`, `internal/api/telemetry_handler.go`, `internal/observability/metrics.go`, `internal/executor/proxy.go`, and `guest-runner/main.go`; expanded Python SDK tests across HTTP transport, result/receipt wrappers, verifier failure modes, and error mapping; expanded TypeScript SDK tests across `HTTPTransport`, `ExecutionResult`, `Receipt`, `ReceiptVerifier`, and explicit error/summary parsing behavior
- Verified: `cd ~/aegis && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go test -coverprofile=/tmp/api_cov.out ./internal/api && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go tool cover -func=/tmp/api_cov.out`; `cd ~/aegis && env PATH=/home/cellardoor/local/go/bin:/home/cellardoor/go/bin:/usr/bin:/bin /home/cellardoor/go/bin/staticcheck ./internal/api ./internal/executor ./internal/observability`; `cd ~/aegis && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go vet ./internal/api ./internal/executor ./internal/observability`; `cd ~/aegis/guest-runner && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go test ./... && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go vet ./...`; `cd ~/aegis && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go test -cover ./internal/policy/divergence && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go test -cover ./internal/policy/evaluator`; `cd ~/aegis/sdk/python && .venv/bin/python -m unittest tests.test_sdk && .venv/bin/coverage run -m unittest discover -s tests && .venv/bin/coverage report -m aegis/*.py`; `cd C:\Users\Cellardoor\aegis\sdk\typescript && npm run lint && npm run build && npx c8 --reporter=text node --test dist/tests/sdk.test.js`; reran `npm audit --audit-level=low` and `sdk/python/.venv/bin/pip-audit`
- Skipped: no broad guest-runner/runtime-trace cleanup beyond the highest-signal ignored-error sites; retained best-effort cleanup/kill sites remain and should be reviewed in a separate narrow pass if the bar moves from “launch with caveats” to stricter runtime hygiene

## 2026-04-10 Documentation & Launch Materials
- Implemented: rewrote the root `README.md` as a product-grade OSS landing page centered on execution evidence; added `docs/quickstart.md`, `docs/architecture.md`, `docs/api.md`, and `docs/openapi.json`; added `SECURITY.md`; rewrote `docs/mcp_server.md` and `docs/warm_pool.md`; added `sdk/python/README.md`; rewrote `sdk/typescript/README.md`; added a careful category-based comparison section for Aegis, E2B, Docker Sandboxes, and managed-agent offerings
- Verified: `py -3 -m json.tool docs/openapi.json`; route validation against `cmd/orchestrator/main.go`; MCP tool/schema validation against `internal/mcp/tools.go`; CLI command validation against `cmd/aegis-cli/main.go`; `cmd /c "cd /d C:\Users\Cellardoor\aegis\sdk\typescript && npm run build"`; `cd ~/aegis && GOTOOLCHAIN=go1.25.9 /home/cellardoor/local/go/bin/go test ./cmd/aegis-cli ./cmd/orchestrator ./internal/mcp ./internal/api`; `cd ~/aegis/sdk/python && .venv/bin/python -m unittest tests.test_sdk`; direct WSL import check for `from aegis import AegisClient, ExecutionRequest, Receipt`
- Skipped: no generated docs site, no docs framework adoption, and no roadmap claims beyond explicit not built yet boundaries; comparison entries stayed cautious where competitor products are not directly equivalent to Aegiss execution-evidence model

## 2026-04-10 MCP tools/list compatibility fix
- Implemented: relaxed `internal/mcp/server.go` so `tools/list` accepts omitted params, `{}`, and optional cursor-style params via a loose `toolsListParams` decoder; ignored cursor when pagination is not implemented; added compatibility coverage in `internal/mcp/server_test.go`
- Verified: `cd ~/aegis && /home/cellardoor/local/go/bin/gofmt -w internal/mcp/server.go internal/mcp/server_test.go`; `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/mcp ./cmd/aegis-mcp`; `cd ~/aegis && /home/cellardoor/local/go/bin/go build -buildvcs=false -o ./bin/aegis-mcp ./cmd/aegis-mcp`; live stdio check against `./bin/aegis-mcp` returned successful `tools/list` responses for omitted params, `{}`, and `{ "cursor": "next" }`
- Skipped: Codex desktop restart and `/mcp` UI confirmation were not performed from this shell, so compatibility was validated at the MCP protocol/binary level only
## 2026-04-10 TypeScript broker path regression fix
- Implemented:
  - fixed host broker listener deadlock by decoding broker requests directly from the live vsock connection instead of reading until EOF first
  - added executor regression test proving broker responses return before the client closes its side of the connection
- Verified:
  - `/home/cellardoor/local/go/bin/go test ./internal/executor -run TestHandleBrokerConnRespondsWithoutClientClosing -count=1` passed
  - `curl -fsS http://127.0.0.1:8080/v1/health` passed in WSL after restart
  - `node dist/examples/broker_allowed.js` against `http://172.30.137.226:8080` passed with marker `PASS_auth_present_true_no_raw_token`, `receiptVerified=true`, `upstreamRequests=1`, telemetry `credential.request,credential.allowed`
  - `node dist/examples/broker_denied.js` against `http://172.30.137.226:8080` passed with marker `PASS_denied_no_raw_token`, `receiptVerified=true`, `upstreamRequests=0`, telemetry `credential.request,credential.denied`
  - proof bundle scan for both executions confirmed no raw broker token leak in stdout, stderr, or proof files
- Skipped:
  - Windows Node validation on `http://127.0.0.1:8080`; this desktop thread still cannot reach the WSL server on localhost even though WSL-local health and the reachable WSL IP path both work

## 2026-04-10 MCP integration hardening
- Implemented:
  - fixed MCP Python/Node default contract to avoid the shell-launcher `process.shell_disallowed` path by zeroing guest PID launcher limits for non-shell languages
  - surfaced broker evidence through MCP execute/verify results and receipt summaries
  - surfaced warm-pool diagnostics in MCP results and forwarded warm-pool env vars through `start-local-orchestrator.sh`
  - hardened MCP broker delegation contract with required broker resources, allowed domains/action types, localhost proxy allowance, and explicit binding-miss denial
  - expanded MCP Python runtime baseline reads to include `/tmp` so guest transient script/current-dir reads no longer fail the contract
  - added targeted tests for MCP intent construction, broker result shaping, warm diagnostics, Python PID handling, broker binding denial, and Python runtime baseline reads
- Verified:
  - `gofmt -w internal/api/handler.go internal/api/helpers_test.go internal/mcp/tools.go internal/mcp/tools_test.go internal/broker/broker.go internal/broker/broker_test.go internal/policy/evaluator/evaluator.go internal/policy/evaluator/evaluator_test.go internal/receipt/bundle.go guest-runner/main.go guest-runner/main_test.go`
  - `go test ./internal/api ./internal/mcp ./internal/broker ./internal/policy/evaluator ./internal/receipt ./guest-runner/...`
  - rebuilt `bin/aegis-mcp`, `bin/orchestrator`, and rebaked `guest-runner` into the rootfs
  - live MCP validation proved:
    - simple Python execution now succeeds without `process.shell_disallowed`
    - broker allowed path surfaces `credential.request,credential.allowed`, verifies receipts, shows one upstream authenticated request, and leaks no raw token
    - broker denied path surfaces `credential.request,credential.denied`, verifies receipts, shows zero upstream authenticated requests, and leaks no raw token
    - warm-pool diagnostics now show real warm-vs-cold dispatch evidence instead of relying on end-to-end duration
    - Python `json.dumps(...)` now succeeds through MCP after admitting `/tmp` as a baseline read path
- Skipped:
  - full clean-vs-malicious Python workflow closeout, because Python file-write/workspace workflows still time out under `allow` with verified receipts and no divergence rule hits

## 2026-04-10 MCP Python workspace runtime investigation
- Implemented:
  - narrowed the remaining MCP blocker from ?Python touching `/workspace` hangs? to ?any workspace-backed Python MCP execution hangs, even `print(...)` with `allow_write_paths` set`
  - reverted the attempted Python/Node `/workspace` guest working-directory change after proving it makes workspace-backed Python fail even without file I/O
  - added and kept a safer post-open fd symlink inspection path in `guest-runner/runtime_trace.go` with matching tests; it preserves the writable-mount symlink guard but did not resolve the workspace-backed Python hang
- Verified:
  - `cd ~/aegis/guest-runner && /home/cellardoor/local/go/bin/gofmt -w main.go main_test.go runtime_trace.go runtime_trace_test.go && /home/cellardoor/local/go/bin/go test ./...`
  - `cd ~/aegis && /home/cellardoor/local/go/bin/go test ./internal/mcp ./cmd/aegis-mcp`
  - `cd ~/aegis && ./scripts/rebake-guest-runner.sh`
  - live MCP execution `53217efb-7011-4b59-9168-d0fe67cd9633` proved minimal Python `open('/workspace/out.txt','w')` still timed out with verified receipt and `divergence_verdict=allow`
  - live MCP execution `30bf865b-d4f6-4197-81d0-7a5dab59ec29` proved even `print('workspace-mounted-ok')` times out when `allow_write_paths` triggers a workspace-backed context
  - after reverting Python/Node guest cwd back to `/tmp`, live MCP execution `b18c1596-a401-4513-85f4-11d614ec8243` still failed in a workspace-backed context with verified receipt and `divergence_verdict=allow`
  - simple non-workspace Python MCP execution still works: `59bf65d8-7b73-497f-b1ec-9a2301a4c77b` returned `mcp-python-ok` with verified receipt
- Skipped:
  - final clean-vs-malicious Python workflow closeout, because the deeper workspace-backed Python runtime hang is still unresolved and I am not going to relabel that as a passing phase


## 2026-04-10 MCP Python runtime hang follow-up
- Implemented:
  - added timeout diagnostics in `guest-runner/main.go` so timed-out Python executions report `/proc/<pid>` state, `wchan`, and `syscall`
  - proved the current failure is no longer MCP-shaping-specific by reproducing timeouts in WSL-local MCP runs for simple no-workspace Python
  - reverted the post-open `/proc/<pid>/fd` tracer inspection experiment and reverted the temporary Python `-I -B` startup flags after they failed to restore correct execution
- Verified:
  - `cd ~/aegis/guest-runner && /home/cellardoor/local/go/bin/go test ./...`
  - rebaked the guest-runner into `assets/alpine-base.ext4` and restarted the local orchestrator repeatedly during diagnosis
  - WSL-local MCP execution `07c63f3a-8105-45ed-9987-1f38fd95caeb` timed out with `State=D`, `wchan=__lock_page_or_retry`
  - WSL-local MCP execution `cd0e0fe5-d3f6-41cd-aada-aa075407f38b` timed out with `State=t`, `wchan=bh_submit_read`, `syscall=262`
  - after reverting the tracer fd-inspection path, WSL-local MCP execution `6bc45573-5fe4-42d5-8ecf-b946e171742b` still timed out, now with `State=R`, `syscall=running`
  - after reverting the temporary Python startup flags, WSL-local MCP execution `0eb3a8eb-dbce-4e5e-85ce-2d660faae437` still timed out with `State=D`, `wchan=__wait_on_buffer`, `syscall=217`
- Skipped:
  - final acceptance validation for clean `/tmp` write/read and malicious `os.system("curl ...")`, because the runtime still cannot complete the baseline no-workspace Python MCP execution reliably on the current guest-runner branch

## 2026-04-12 Docs architecture diagram refresh
- Implemented:
  - reviewed the updated `docs/architecture-diagram.png` and confirmed it is the only working-tree change
  - prepared the repo for a docs-only commit by recording this session state
- Verified:
  - visually inspected `docs/architecture-diagram.png`
  - `git -c safe.directory=C:/Users/Cellardoor/projects/Aegis status --short --branch` showed only `docs/architecture-diagram.png` modified on `main`
  - `git -c safe.directory=C:/Users/Cellardoor/projects/Aegis diff --stat -- docs/architecture-diagram.png` confirmed a binary-only image replacement
- Skipped:
  - no code, API, or SDK validation, because this session only stages a documentation image update
