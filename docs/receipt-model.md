# Receipt Model

The canonical public receipt document is [receipt-schema.md](receipt-schema.md).

Use it for:

- signed bundle structure
- predicate fields
- verification behavior
- current proof limits
