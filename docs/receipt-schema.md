# Receipt Schema (Deprecated)

Deprecated. Do not use this as the current Aegis receipt guide.

The current public receipt document is [receipt-model.md](receipt-model.md).

For the raw predicate contract, use [../schemas/receipt-predicate-v1.json](../schemas/receipt-predicate-v1.json).

The older field-by-field schema narrative was retired so the repo only tells one current receipt story.
